version: '3.4'

services:

  mongo:
    image: 'mongo'
    ports:
      - 27017:27017

  cassandra:
    image: 'cassandra'
    ports:
      - 9042:9042

  neo4j:
    image: 'neo4j'
    ports:
      - 7474:7474
      - 7687:7687
    environment:
      - NEO4J_AUTH=none

  movies-app:
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      - mongo
      - cassandra
      - neo4j